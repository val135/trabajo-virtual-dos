let boolean = "true";
let fullWidth= 60;
let padding = 5;
let border = 2;



const modelbox=(B, F, P, O)=>{
    if (boolean == true){
        return ancho;
    }
    else {
        return padding;
    }
}

modelbox(boolean,fullWidth,padding,border);


/*function theBox(C,F,P,B){

    if (C == "true") {
        return (C - (B*2)(C*4));
    }  else if (C == "false") {
        return (C);
    }
}

console.log(theBox(contentBox,fullWidth,padding,border));*/
